<Window x:Class="PhoenixSwitcher.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhoenixSwitcher.Windows"
        xmlns:adonisUI="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:viewlibrary="clr-namespace:CosntCommonViewLibrary.XmlEditor;assembly=CosntCommonViewLibrary"
        xmlns:settingscontrol="clr-namespace:CosntCommonViewLibrary.SettingsControl;assembly=CosntCommonViewLibrary"
        mc:Ignorable="d"
        Title="{Binding WindowName}"  Height="540" Width="540">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Margin="10,10,10,5" BorderThickness="2"
            BorderBrush="{DynamicResource {x:Static adonisUI:Brushes.Layer0BorderBrush}}">
            <Button Click="Save_Click" HorizontalAlignment="Left" MinWidth="100">
                <TextBlock Text="{Binding SaveButtonText}"/>
            </Button>
        </Border>
        <Border Grid.Row="1" Margin="10,5,10,10" BorderThickness="2"
            BorderBrush="{DynamicResource {x:Static adonisUI:Brushes.Layer1BorderBrush}}">
            <settingscontrol:SettingsWindow TabItems="{Binding SettingsItemList}" />
        </Border>
    </Grid>
</Window>
