<Window x:Class="PhoenixSwitcher.Windows.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhoenixSwitcher.Windows"
        xmlns:adonisUI="clr-namespace:AdonisUI;assembly=AdonisUI"
        mc:Ignorable="d"
        Title="{Binding WindowName}" Height="360" Width="640">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>
    <Grid>
        <Border BorderThickness="2" Margin="5"
            BorderBrush="{DynamicResource {x:Static adonisUI:Brushes.Layer1BorderBrush}}"
            Background="{DynamicResource {x:Static adonisUI:Brushes.Layer1BackgroundBrush}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                
                <Image Grid.Row="0" Grid.Column="0" Width="128" Height="128" VerticalAlignment="Top" Margin="10"
                    Source="/Resources/Images/Phoenix_Icon.png" />
                
                <Separator Grid.Column="1" Width="5" Visibility="Hidden"/>
                
                <Grid Grid.Row="0" Grid.Column="2"  Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="{Binding VersionText}" FontSize="30"/>
                    <TextBlock Grid.Row="1" Text="{Binding CopyrightText}" FontSize="30"/>
                    <TextBlock Grid.Row="2" Text="{Binding CreatorText}" FontSize="30"/>
                </Grid>
                
                <Separator Grid.Row="1" Grid.Column="0" Height="5" Visibility="Hidden"/>
                
                <Image Grid.Row="2" Grid.Column="0" Width="128" Height="128" VerticalAlignment="Top" Margin="10"
                    Source="/Resources/Images/CNH_COSnT_Icon.png" />
            </Grid>
        </Border>
        
    </Grid>
</Window>
