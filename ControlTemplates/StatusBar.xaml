<UserControl x:Class="PhoenixSwitcher.ControlTemplates.StatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhoenixSwitcher.ControlTemplates"
             xmlns:viewmodels="clr-namespace:PhoenixSwitcher.ViewModels"
             xmlns:adonisUI="clr-namespace:AdonisUI;assembly=AdonisUI"
             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
             mc:Ignorable="d">
    <Border
        Height="100"
        MaxHeight="100"
        BorderThickness="2"
        Background="{DynamicResource {x:Static adonisUI:Brushes.Layer1BackgroundBrush}}"
        BorderBrush="{DynamicResource {x:Static adonisUI:Brushes.Layer1BorderBrush}}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60*"/>
                <RowDefinition Height="40*"/>
            </Grid.RowDefinitions>
            <!-- Main Status -->
            <Border
                Grid.Row="0"
                BorderThickness="2"
                Background="{DynamicResource {x:Static adonisUI:Brushes.Layer1BackgroundBrush}}"
                BorderBrush="{DynamicResource {x:Static adonisUI:Brushes.Layer1BorderBrush}}">
                <Grid>
                    <ProgressBar 
                        Minimum="0"
                        Maximum="100"
                        Value="{Binding MainStatusPercentage}"
                        Padding="0,2">
                    </ProgressBar>
                    <Viewbox 
                        Margin="0,0,0,5">
                        <TextBlock Text="{Binding MainStatusText}"/>
                    </Viewbox>
                </Grid>
            </Border>
            <!-- Sub status -->
            <Border
                Grid.Row="1"
                BorderThickness="2"
                Background="{DynamicResource {x:Static adonisUI:Brushes.Layer1BackgroundBrush}}"
                BorderBrush="{DynamicResource {x:Static adonisUI:Brushes.Layer1BorderBrush}}"
                Visibility="{Binding L1StatusVisibility}">
                <Grid>
                    <ProgressBar 
                        Minimum="0"
                        Maximum="100"
                        Value="{Binding L1StatusPercentage}"
                        Padding="0,2">
                    </ProgressBar>
                    <Viewbox 
                        Margin="0,0,0,5">
                        <TextBlock Text="{Binding L1StatusText}"/>
                    </Viewbox>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
