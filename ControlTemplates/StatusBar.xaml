<UserControl x:Class="PhoenixSwitcher.ControlTemplates.StatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhoenixSwitcher.ControlTemplates"
             xmlns:viewmodels="clr-namespace:PhoenixSwitcher.ViewModels"
             xmlns:adonisUI="clr-namespace:AdonisUI;assembly=AdonisUI"
             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
             xmlns:converters="clr-namespace:PhoenixSwitcher.ValueConverters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:FontSizeForTextConverter x:Key="FontSizeForTextConverter" MinFontSize="6" MaxFontSize="76" Multiplier="0.85" />
    </UserControl.Resources>
    <Border BorderThickness="2"
        Background="{DynamicResource {x:Static adonisUI:Brushes.Layer1BackgroundBrush}}"
        BorderBrush="{DynamicResource {x:Static adonisUI:Brushes.Layer1BorderBrush}}">
        <TextBlock x:Name="StatusTextBlock"
           Text="{Binding MainStatusText}"
           TextWrapping="Wrap"
           Foreground="{Binding StatusColor}"
           TextAlignment="Left"
           HorizontalAlignment="Stretch"
           VerticalAlignment="Center"
           Margin="5,0">
            <TextBlock.FontSize>
                <MultiBinding Converter="{StaticResource FontSizeForTextConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                    <Binding Path="ActualHeight" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                    <Binding Path="Text" RelativeSource="{RelativeSource Self}" />
                </MultiBinding>
            </TextBlock.FontSize>
        </TextBlock>
    </Border>
</UserControl>
