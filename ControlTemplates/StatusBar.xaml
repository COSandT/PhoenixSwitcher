<UserControl x:Class="PhoenixSwitcher.ControlTemplates.StatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhoenixSwitcher.ControlTemplates"
             xmlns:viewmodels="clr-namespace:PhoenixSwitcher.ViewModels"
             xmlns:adonisUI="clr-namespace:AdonisUI;assembly=AdonisUI"
             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
             mc:Ignorable="d">
    <Border
        Height="100"
        MaxHeight="100"
        Background="{DynamicResource {x:Static adonisUI:Brushes.Layer1BackgroundBrush}}"
        BorderBrush="{DynamicResource {x:Static adonisUI:Brushes.Layer2BorderBrush}}"
        BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60*"/>
                <RowDefinition Height="40*"/>
            </Grid.RowDefinitions>
            <Border>
                <ProgressBar 
                    adonisExtensions:ProgressBarExtension.Content="{Binding MainStatusText}"
                    Minimum="0"
                    Value="{Binding MainStatusPercentage}"
                    Maximum="100"
                    Padding="0, 2">
                </ProgressBar>
            </Border>
            <Border
                Grid.Row="1"
                Background="{DynamicResource {x:Static adonisUI:Brushes.Layer2BackgroundBrush}}"
                BorderBrush="{DynamicResource {x:Static adonisUI:Brushes.Layer3BorderBrush}}"
                BorderThickness="2"
                Visibility="{Binding L1StatusVisibility}">
                <ProgressBar 
                    adonisExtensions:ProgressBarExtension.Content="{Binding L1StatusText}"
                    Minimum="0"
                    Value="{Binding L1StatusPercentage}"
                    Maximum="100"
                    Padding="0, 2">
                </ProgressBar>
            </Border>
        </Grid>
    </Border>
</UserControl>
