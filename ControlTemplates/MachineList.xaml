<UserControl x:Class="PhoenixSwitcher.ControlTemplates.MachineList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhoenixSwitcher.ControlTemplates"
             xmlns:viewmodels="clr-namespace:PhoenixSwitcher.ViewModels"
             xmlns:adonisUI="clr-namespace:AdonisUI;assembly=AdonisUI"
             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>
    <Grid Background="{DynamicResource {x:Static adonisUI:Brushes.Layer1BackgroundBrush}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Machine List Header-->
        <Grid 
            Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ToggleButton
                Grid.Column="0"
                Width="25"
                Height="25"
                Margin="5"
                VerticalAlignment="Center"
                IsChecked="{Binding MachineListExpander, Mode=TwoWay}">
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton">
                        <Setter Property="Content" Value="▷"/>
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content" Value="◁"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>
            <Border
                Grid.Column="1"
                Background="{DynamicResource {x:Static adonisUI:Brushes.Layer2BackgroundBrush}}">
                <Viewbox
                    Margin="0,5,10,5"
                    Visibility="{Binding MachineListExpander, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="{Binding MachineListHeaderText}" />
                </Viewbox>
            </Border>
        </Grid>
        <!-- Scan Machine Box -->
        <Grid Grid.Row="1"
                Visibility="{Binding MachineListExpander, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
            <TextBox
                x:Name="ScannedMachineText"
                KeyUp="ScannedMachineText_KeyUp">
            </TextBox>
            <TextBlock
                Text="{Binding SelectToScanText}"
                Foreground="Gray"
                FontStyle="Oblique"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IsHitTestVisible="False"
                Visibility="{Binding ElementName=ScannedMachineText, Path=IsFocused, Converter={StaticResource BoolToVisibility}, ConverterParameter=0}">
            </TextBlock>
        </Grid>
        <!--Machine List-->
        <Grid 
            Grid.Row="2" 
            Visibility="{Binding MachineListExpander, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
            <ScrollViewer 
                    Margin="0,1.5,1.5,0" 
                    HorizontalAlignment="Stretch" 
                    VerticalAlignment="Stretch" 
                    HorizontalScrollBarVisibility="Auto" 
                    VerticalScrollBarVisibility="Auto" >
                <StackPanel x:Name="VehicleList" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
